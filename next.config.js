/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['4.bp.blogspot.com', 'www.blogger.com', 'https'],
    
  },
}

module.exports = nextConfig
